<%= form_for(@club_type) do |f| %>
  <% if @club_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club_type.errors.count, "error") %> prohibited this club_type from being saved:</h2>

      <ul>
      <% @club_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
